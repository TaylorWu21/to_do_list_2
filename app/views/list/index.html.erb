<h1>List of Tasks</h1>
<br />

<% if @lists.any? %>
	<ul>
	<% @lists.each do |list| %>
	<li>
		<%= link_to list.task, list_path(list) %>
	</li>
	<% end %>
	</ul>
<% else %>
	<h3>No lists found. Create one</h3>
<% end %>

<%= link_to 'New List', new_list_path %>